const toolBtns=document.querySelectorAll(".tool-btn");function setTool(name){currentTool=name;toolBtns.forEach(b=>b.classList.toggle("active",b.id===name+"Btn"));document.getElementById("mixerPanel").style.display=name==="mixer"?"block":"none"}
document.getElementById("brushBtn").onclick=()=>setTool("brush");document.getElementById("mixerBtn").onclick=()=>setTool("mixer");document.getElementById("eraserBtn").onclick=()=>setTool("eraser");document.getElementById("fitBtn").onclick=fitToScreen;window.addEventListener("keydown",e=>{if(e.target.tagName==="INPUT")return;if(e.code==="Space"&&!e.repeat){spaceHeld=!0;canvasArea.style.cursor="grab";return}
if(e.key==="b"||e.key==="B")setTool("brush");if(e.key==="m"||e.key==="M")setTool("mixer");if(e.key==="e"||e.key==="E")setTool("eraser");if((e.ctrlKey||e.metaKey)&&e.key==="z"){e.preventDefault();document.getElementById("undoBtn").click()}
if((e.ctrlKey||e.metaKey)&&(e.key==="y"||(e.shiftKey&&e.key==="z"))){e.preventDefault();document.getElementById("redoBtn").click()}});window.addEventListener("keyup",e=>{if(e.code==="Space"){spaceHeld=!1;canvasArea.style.cursor=""}});document.getElementById("sizeSlider").oninput=e=>{brushSize=Number(e.target.value);document.getElementById("sizeValue").textContent=brushSize;updateBrushCursorSize()};document.getElementById("opacitySlider").oninput=e=>{brushOpacity=Number(e.target.value)/100;document.getElementById("opacityValue").textContent=e.target.value+"%"};document.getElementById("mixSlider").oninput=e=>{mixRatio=Number(e.target.value)/100;document.getElementById("mixValue").textContent=e.target.value+"%"};document.getElementById("wetSlider").oninput=e=>{wetRatio=Number(e.target.value)/100;document.getElementById("wetValue").textContent=e.target.value+"%"};let _prevTool="brush";let _eyeActive=!1;const loupe=document.getElementById("eyedropperLoupe");const _origSetTool=setTool;window.setTool=function(name){if(name!=="eyedropper"&&currentTool!=="eyedropper")_prevTool=name;_origSetTool(name);if(name==="eyedropper"){canvasArea.style.cursor="crosshair"}else{canvasArea.style.cursor="";loupe.style.display="none"}};setTool=window.setTool;document.getElementById("eyedropperBtn").onclick=()=>setTool("eyedropper");window.addEventListener("keydown",e=>{if(e.target.tagName==="INPUT")return;if(e.key==="i"||e.key==="I"){if(currentTool==="eyedropper")setTool(_prevTool);else setTool("eyedropper")}});function samplePixelAt(cx,cy){const x=Math.max(0,Math.min(Math.round(cx),colorCanvas.width-1));const y=Math.max(0,Math.min(Math.round(cy),colorCanvas.height-1));const cd=colorCtx.getImageData(x,y,1,1).data;const ld=lineCtx.getImageData(x,y,1,1).data;const la=ld[3]/255;const ca=cd[3]/255;const outA=la+ca*(1-la);if(outA<0.01)return null;const r=Math.round((ld[0]*la+cd[0]*ca*(1-la))/outA);const g=Math.round((ld[1]*la+cd[1]*ca*(1-la))/outA);const b=Math.round((ld[2]*la+cd[2]*ca*(1-la))/outA);return{r,g,b}}
function updateLoupe(e){const rect=canvasArea.getBoundingClientRect();const canvasX=(e.clientX-rect.left-panX)/zoom;const canvasY=(e.clientY-rect.top-panY)/zoom;const px=samplePixelAt(canvasX,canvasY);const OFFSET=20;const loupeW=loupe.offsetWidth||80;const loupeH=loupe.offsetHeight||90;const vpW=window.innerWidth;let lx=e.clientX+OFFSET;let ly=e.clientY-OFFSET-loupeH;if(lx+loupeW>vpW)lx=e.clientX-OFFSET-loupeW;if(ly<0)ly=e.clientY+OFFSET;loupe.style.left=lx+"px";loupe.style.top=ly+"px";loupe.style.display="block";const swatch=document.getElementById("loupeColor");const label=document.getElementById("loupeHex");if(px){const hex="#"+[px.r,px.g,px.b].map(v=>v.toString(16).padStart(2,"0")).join("");swatch.style.background=hex;label.textContent=hex.toUpperCase();loupe.dataset.hex=hex;loupe.classList.remove("loupe--empty")}else{swatch.style.background="transparent";label.textContent="—";loupe.dataset.hex="";loupe.classList.add("loupe--empty")}}
function applyPickedColor(hex){if(!hex)return;brushColor=hex;const r=parseInt(hex.slice(1,3),16);const g=parseInt(hex.slice(3,5),16);const b=parseInt(hex.slice(5,7),16);[cwHue,cwSat,cwVal]=rgbToHsv(r,g,b);redrawWheel();document.getElementById("hexInput").value=hex.toUpperCase();document.getElementById("hexSwatch").style.background=hex;loupe.classList.add("loupe--picked");setTimeout(()=>loupe.classList.remove("loupe--picked"),280)}
canvasArea.addEventListener("pointermove",e=>{if(currentTool!=="eyedropper")return;updateLoupe(e)});canvasArea.addEventListener("pointerdown",e=>{if(currentTool!=="eyedropper")return;if(e.button!==0)return;_eyeActive=!0;updateLoupe(e);e.stopImmediatePropagation()},!0);canvasArea.addEventListener("pointerup",e=>{if(currentTool!=="eyedropper"||!_eyeActive)return;_eyeActive=!1;const hex=loupe.dataset.hex;if(hex){applyPickedColor(hex);setTool(_prevTool)}
e.stopImmediatePropagation()},!0);canvasArea.addEventListener("pointerleave",()=>{if(currentTool==="eyedropper")loupe.style.display="none"})
